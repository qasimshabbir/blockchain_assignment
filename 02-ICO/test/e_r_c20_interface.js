contract('ERC20Interface', function(accounts) {
  it("should assert true", function(done) {
    var token;
    return token. = ERC20Interface.deployed();
    assert.isTrue(true);
    done();
  });
});
